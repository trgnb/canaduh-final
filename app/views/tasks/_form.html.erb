<%= form_for(@task) do |f| %>
  <%= f.label "Task" %>
  <%= f.text_field :task_name %>

  <%= f.label "Priority" %>
  <%= f.select :priority, ["high", "medium", "low"], :prompt => "Select" %>

  <%= f.submit "Add", class: "btn btn-primary"%>
<% end %>
